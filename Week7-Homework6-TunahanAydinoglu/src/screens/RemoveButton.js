

let RemoveButton = (props)=>{
    
    return (
        <div>
           <button className="btn btn-danger" onClick={props.silmeFonksiyonu}>Sil</button>
        </div>
    );
}

export default RemoveButton;